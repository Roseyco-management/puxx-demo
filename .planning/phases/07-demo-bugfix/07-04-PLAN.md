---
phase: 07-demo-bugfix
plan: 04
type: execute
wave: 1
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - app/(portal)/portal/page.tsx
  - app/(portal)/portal/products/page.tsx
  - lib/seo/metadata.ts
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, PROD-01, RETAIL-03, FULFL-02, CUST-01, MOB-01]
gap_closure: true

must_haves:
  truths:
    - "Portal root URL /portal loads without 500 — redirects to /portal/orders"
    - "Portal products page /portal/products renders product catalogue without 500 (no Drizzle)"
    - "Browser request to /site.webmanifest returns valid JSON manifest (no 404 for manifest link)"
  artifacts:
    - path: "app/(portal)/portal/page.tsx"
      provides: "Portal root redirect to working orders page"
      contains: "redirect('/portal/orders')"
    - path: "app/(portal)/portal/products/page.tsx"
      provides: "Product catalogue via Supabase REST"
      contains: "getSupabaseClient"
    - path: "lib/seo/metadata.ts"
      provides: "Correct manifest reference"
      contains: "'/site.webmanifest'"
  key_links:
    - from: "app/(portal)/portal/page.tsx"
      to: "/portal/orders"
      via: "Next.js redirect()"
      pattern: "redirect\\('/portal/orders'\\)"
    - from: "app/(portal)/portal/products/page.tsx"
      to: "lib/db/supabase.ts"
      via: "getSupabaseClient().from('products')"
      pattern: "getSupabaseClient.*from\\('products'\\)"
    - from: "lib/seo/metadata.ts"
      to: "public/site.webmanifest"
      via: "manifest field in baseMetadata"
      pattern: "manifest.*site\\.webmanifest"
---

<objective>
Close two verification gaps from 07-VERIFICATION.md: fix the portal entry point 500 (Drizzle on products page) and the manifest.json 404 (metadata points to wrong filename).

Purpose: These are the final two gaps preventing a clean demo — Gap 1 is a BLOCKER (retailer portal crashes on entry), Gap 2 is a WARNING (console 404 visible to demo reviewers).
Output: All demo critical path pages render without errors.
</objective>

<execution_context>
@/Users/arnispiekus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnispiekus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-demo-bugfix/07-VERIFICATION.md
@.planning/phases/07-demo-bugfix/07-01-SUMMARY.md

<interfaces>
<!-- Established patterns from prior plans that executor must follow exactly -->

From lib/db/supabase.ts:
```typescript
export function getSupabaseClient(): SupabaseClient;
// Usage: getSupabaseClient().from('products').select('*').eq('is_active', true)
```

From lib/auth/session.ts:
```typescript
export async function getSession(): Promise<{ user: { id: string; email: string; role: string; name?: string } } | null>;
```

From app/api/admin/products/route.ts (working pattern — Supabase REST for products):
```typescript
const supabase = getSupabaseClient();
const { data: allProducts, error } = await supabase
  .from('products')
  .select('*')
  .order('created_at', { ascending: false });
```

Supabase REST returns snake_case columns: `name`, `nicotine_strength`, `price`, `stock_quantity`, `is_active`, `image_url`.
The current Drizzle portal page uses camelCase: `product.nicotineStrength`, `product.stockQuantity`, `product.isActive`.
Executor MUST update template references to snake_case when switching to Supabase REST.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix portal products page (Drizzle to Supabase REST) and redirect</name>
  <files>app/(portal)/portal/products/page.tsx, app/(portal)/portal/page.tsx</files>
  <action>
**app/(portal)/portal/products/page.tsx** — Migrate from Drizzle to Supabase REST:

1. Remove imports: `getDb` from `@/lib/db/drizzle`, `products` from `@/lib/db/schema`, `eq` from `drizzle-orm`
2. Add imports: `getSupabaseClient` from `@/lib/db/supabase`
3. Replace the data fetch in `PortalProductsPage`:
   - Remove: `const db = getDb(); const allProducts = await db.select().from(products).where(eq(products.isActive, true));`
   - Replace with:
     ```typescript
     const supabase = getSupabaseClient();
     const { data: allProducts, error } = await supabase
       .from('products')
       .select('*')
       .eq('is_active', true)
       .order('name', { ascending: true });
     ```
   - Add error handling: if error, set allProducts to empty array
4. Update ALL template references from camelCase (Drizzle schema) to snake_case (Supabase REST):
   - `product.nicotineStrength` -> `product.nicotine_strength`
   - `product.stockQuantity` -> `product.stock_quantity`
   - `product.price` stays as `product.price` (same in both)
   - `product.name` stays as `product.name` (same in both)
   - `product.id` stays as `product.id` (same in both)
5. Keep `export const dynamic = 'force-dynamic'` at the top
6. Keep all existing UI components (Table, Card, Badge imports), layout, and trade price calculation `(parseFloat(product.price) * 0.8).toFixed(2)` unchanged

**app/(portal)/portal/page.tsx** — Change redirect target:
- Change `redirect('/portal/products')` to `redirect('/portal/orders')`
- This makes the natural retailer landing page their order history (the working page), not the catalogue

Why both changes: The redirect alone would hide the broken products page but nav links in portal/layout.tsx still point to /portal/products. Migrating the products page ensures every portal route works. The redirect change makes the default landing page the more useful orders view.
  </action>
  <verify>
    <automated>cd /Users/arnispiekus/Work/Github/Puxx && grep -c "getDb\|drizzle" app/\(portal\)/portal/products/page.tsx; echo "---"; grep "getSupabaseClient" app/\(portal\)/portal/products/page.tsx; echo "---"; grep "redirect" app/\(portal\)/portal/page.tsx; echo "---"; grep "snake_case_check:" && grep -c "nicotineStrength\|stockQuantity\|isActive" app/\(portal\)/portal/products/page.tsx; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- `app/(portal)/portal/products/page.tsx` has zero Drizzle imports (getDb, drizzle-orm, schema)
- `app/(portal)/portal/products/page.tsx` uses `getSupabaseClient().from('products')` with snake_case field refs
- `app/(portal)/portal/page.tsx` redirects to `/portal/orders`
- `npx next build` completes without errors related to these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix manifest metadata reference</name>
  <files>lib/seo/metadata.ts</files>
  <action>
In `lib/seo/metadata.ts`, line 34, change:
```typescript
manifest: '/manifest.json',
```
to:
```typescript
manifest: '/site.webmanifest',
```

This aligns the Next.js metadata manifest declaration with the actual file at `public/site.webmanifest`. The middleware already excludes manifest files from region redirects (established in 07-01), so the file will be served directly.

Do NOT rename `public/site.webmanifest` to `public/manifest.json` — `site.webmanifest` is the W3C-recommended filename and the file already exists with correct content.
  </action>
  <verify>
    <automated>cd /Users/arnispiekus/Work/Github/Puxx && grep "manifest" lib/seo/metadata.ts && test -f public/site.webmanifest && echo "webmanifest file exists"</automated>
  </verify>
  <done>
- `lib/seo/metadata.ts` declares `manifest: '/site.webmanifest'`
- `public/site.webmanifest` exists (already confirmed)
- Next.js will emit `<link rel="manifest" href="/site.webmanifest">` which resolves to the real file
  </done>
</task>

</tasks>

<verification>
1. `grep -r "getDb\|from '@/lib/db/drizzle'" app/(portal)/` returns zero matches — no Drizzle imports remain in portal
2. `grep "redirect.*portal" app/(portal)/portal/page.tsx` shows `/portal/orders`
3. `grep "manifest" lib/seo/metadata.ts` shows `/site.webmanifest`
4. `npx next build` completes without TypeScript or import errors
</verification>

<success_criteria>
- Portal root URL redirects to /portal/orders (no 500 on entry)
- Portal products page uses Supabase REST (no Drizzle, no 500 on Vercel)
- Manifest metadata points to existing file (no 404 in browser)
- All 7/7 Phase 7 success criteria truths now pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-demo-bugfix/07-04-SUMMARY.md`
</output>
