---
phase: 02-storefront
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - PROD-01
  - PROD-02
  - CHKOUT-01
  - CHKOUT-02

must_haves:
  truths:
    - "Product catalogue page displays all 12 flavour variants, each with 6 strength options, at the correct base pricing"
    - "Individual product detail page shows images, a flavour/strength selector, and an add-to-cart button"
    - "Age verification gate appears before checkout and blocks progress until the user confirms they are 18+"
    - "A customer can complete an end-to-end checkout for the UK region"
  artifacts:
    - path: "app/[region]/products/page.tsx"
      provides: "Verified working catalogue"
    - path: "app/[region]/products/[slug]/page.tsx"
      provides: "Verified working PDP"
    - path: "app/[region]/checkout/page.tsx"
      provides: "Verified working checkout with age gate"
  key_links:
    - from: "browser"
      to: "/uk/products"
      via: "navigation"
      pattern: "72 products visible"
    - from: "browser"
      to: "/uk/checkout"
      via: "navigation after add-to-cart"
      pattern: "WorldPay label visible"
---

<objective>
Human verification of all four Phase 2 success criteria against a running dev server.

Purpose: Confirms the storefront is demo-ready before Phase 3 begins.
Output: Human sign-off or issue list for gap closure.
</objective>

<execution_context>
@/Users/arnispiekus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arnispiekus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-storefront/02-01-SUMMARY.md
@.planning/phases/02-storefront/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm routes are reachable</name>
  <files></files>
  <action>
    Run `pnpm dev` in the background. Wait for the "Ready" message. Then make three curl checks to verify routes return 200:
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/uk/products`
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/uk/products/mango-ice-4mg`
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/uk/checkout`

    Report the HTTP status codes for each. If any return non-200 (other than redirects to age-gate, which is expected), surface the error from the dev server terminal output.

    Also verify the API is returning seeded products:
    - `curl -s http://localhost:3000/api/products | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8'); console.log(JSON.parse(d).products?.length)"`
    — should return 72.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/uk/products</automated>
  </verify>
  <done>All three routes return 200. /api/products returns 72 products.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 2 success criteria</name>
  <action>Present the verification steps below to the user and wait for their response.</action>
  <verify>
    <automated>MISSING — human visual verification required for UI rendering and interactive flows</automated>
  </verify>
  <done>User types "approved" confirming all four success criteria pass in browser.</done>
  <what-built>
    Phase 2 storefront: region-aware product catalogue, product detail page, cart, checkout with age verification.
    All routes are under /[region]/ (e.g. /uk/products, /ca/checkout).
    ProductCard shows region-correct currency and basePrice.
    Checkout shows correct payment method label per region.
    AgeGate is wired in root layout and blocks all routes until 18+ confirmed.
  </what-built>
  <how-to-verify>
    Start the dev server if not already running: `pnpm dev`

    **SC1 — Product catalogue (PROD-01):**
    1. Visit http://localhost:3000/uk/products
    2. Confirm: age gate modal appears first — click "I am 18 or older" to proceed
    3. Confirm: product grid shows products with £ pricing (e.g. "£6.00")
    4. Confirm: at least 12 different flavour names visible (Mango Ice, Fresh Mint, Citrus Burst, etc.)
    5. Visit http://localhost:3000/ca/products — confirm CA$ pricing (e.g. "CA$6.50")
    6. Visit http://localhost:3000/us/products — confirm $ pricing (e.g. "$4.99")

    **SC2 — Product detail page (PROD-02):**
    1. From /uk/products, click any product card
    2. Confirm: detail page loads at /uk/products/[slug]
    3. Confirm: product image is visible (placeholder SVG is fine)
    4. Confirm: product name and description are displayed
    5. Confirm: "Add to Cart" button is present and clickable

    **SC3 — Age verification gate (CHKOUT-01):**
    1. Open a fresh private/incognito browser window
    2. Visit http://localhost:3000/uk/products
    3. Confirm: age gate modal appears and blocks the page
    4. Confirm: clicking "I am 18 or older" (or equivalent) dismisses the modal and shows the catalogue
    5. Confirm: refreshing the page does NOT show the age gate again (localStorage persists)

    **SC4 — End-to-end checkout (CHKOUT-02):**
    1. From /uk/products, click Add to Cart on any product
    2. Navigate to http://localhost:3000/uk/cart
    3. Confirm: cart shows the item with £ pricing
    4. Proceed to http://localhost:3000/uk/checkout
    5. Confirm: checkout form loads with contact info, shipping address fields
    6. Confirm: "WorldPay" label is visible in the payment section
    7. Fill in dummy details and submit (or proceed through steps)
    8. Confirm: checkout completes to a confirmation/success state without hard errors
    9. Visit http://localhost:3000/ca/checkout — confirm "Gift Card" label instead of WorldPay
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 success criteria pass. Or describe any issues found (e.g. "SC1 fails — no products showing") so gap closure plans can be created.</resume-signal>
</task>

</tasks>

<verification>
All four Phase 2 success criteria verified by human review:
1. Catalogue: 12 flavours × 6 strengths visible at /uk/products with £ pricing
2. PDP: product detail page loads with image, description, Add to Cart
3. Age gate: modal blocks first visit, localStorage persists after confirmation
4. Checkout: UK checkout reaches confirmation with WorldPay label; CA shows Gift Card
</verification>

<success_criteria>
Human approves all four success criteria. Phase 2 is complete and Phase 3 can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/02-storefront/02-03-SUMMARY.md` following the summary template.
</output>
